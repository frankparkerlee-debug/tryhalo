# Halo Handover — 2026-04-21

Context carry-over for a new chat. Everything below reflects the state of the `/Users/parkerlee/Desktop/Halo/app` repo as of this handover. Pick up where this leaves off without re-deriving history.

---

## 1. Project baseline

- **Stack:** Next.js 16.2.3 + React 19.2.4 + TypeScript, Turbopack. App Router.
- **Infra:** Render (auto-deploy from GitHub `main`). Klaviyo 2024-10-15 API. Upstash Redis (HTTP).
- **Launch window:** ~4 weeks from now.
- **Domain:** tryhalo.co.

### Required env vars (Render)
- `UPSTASH_REDIS_REST_URL`
- `UPSTASH_REDIS_REST_TOKEN`
- `KLAVIYO_PRIVATE_API_KEY`
- `KLAVIYO_LIST_*` (per-list IDs — see `src/lib/klaviyo.ts`)

Status: **not yet confirmed set on Render.** Must verify before production traffic.

---

## 2. What shipped in the prior session

### 2a. Atomic waitlist counter
- `/api/quiz-submission` now calls `reserveWaitlistPosition(email)` from `src/lib/redis.ts` when `quiz === "waitlist_joined"`.
- Position is returned in the API response (`{ ok, id, position }`).
- `StackBuilder.tsx` reads position directly from the submit response — the separate `/api/waitlist-count` fetch was removed.
- Redis key pattern: SHA-256 of email → HGET then INCR+HSET. Idempotent.

### 2b. Reusable `QuizCapture` component
Path: `src/components/quiz/QuizCapture.tsx`

Props: `quiz, answers, derived, primaryProgram, headline, subhead, ctaLabel, showPhone, variant, successSlot, onSuccess, successEventName`

- Engagement tools fire event `estimator_complete`
- Full quizzes fire `quiz_complete`  (**note: `quiz_complete`, not `quiz_completed`** — matches `EventName` union)
- Waitlist fires `waitlist_joined`
- Honeypot + email/phone validation + light/dark variant
- SMS checkbox only renders when phone is entered (TCPA one-to-one consent)
- `answers: answers ?? {}` — required because Zod `.default({})` makes the output non-optional

### 2c. Five engagement tools (config-driven)
Path: `src/lib/engagement-tools.ts` and `src/components/quiz/EngagementTool.tsx`

Config-driven pattern: one generic renderer, five configs.

| Slug | Primary program | Mounted on |
|---|---|---|
| `cellular_age` | NAD+ | **Not mounted** (legacy `CellularAgeQuiz` kept instead — see §2d) |
| `weight_loss_projection` | Weight Loss | `/weight-loss` |
| `trt_drag` | TRT | `/testosterone-therapy` |
| `hrt_severity` | HRT | `/hormone-therapy` |
| `peptides_recovery` | Peptides | `/peptide-therapy` |

State machine: `intro → question:N → capture → reveal`. Auto-advance on chip pick. Fires `estimator_start` on start. Reveal CTA routes to `/stack?from={quiz}&primary={program}&flags=tier_{tier}`.

**Tier enum gotcha:** `QuizDerived["tier"]` is strictly `"strong" | "soft" | "better_elsewhere" | "medical_review"`. Engagement configs use a local `severity: "low" | "medium" | "high"` for display branching, then map: `severity === "high" ? "strong" : "soft"`.

### 2d. `/nad-therapy` — deliberately kept legacy
The legacy 7-question `<CellularAgeQuiz />` (~line 729 of `src/app/nad-therapy/page.tsx`) captures richer signal than the new 4-question `cellular_age` tool: gender, specific age, recovery, sleep, alcohol (CD38 tie-in), supplementation baseline, primary drag. Copy is "Seven quick questions." **Do not replace this.** The `cellular_age` config remains in `engagement-tools.ts` for the `/estimators` QA surface only.

### 2e. `/estimators` QA surface
Path: `src/app/estimators/page.tsx`. `robots: { index: false, follow: false }`. Renders all 5 tools standalone with product-path labels for internal QA.

---

## 3. What's in flight / not done

### Completed since first draft of this handover
- **Runtime QA — done.** Dev server (PID 35544 on port 3001) served every page 200. All 4 engagement tools rendered their eyebrow text on their mounted product pages. `/estimators` rendered all 5. `/nad-therapy` kept the 7-question legacy quiz. API contract validated end-to-end: `{ ok, id, position }`, honeypot returns fake-ok, invalid quiz + missing email rejected with field paths. `/stack` handoff URLs return 200 and render the correct primary program. Typecheck and production build clean (24 routes).
- **One real bug fixed during QA:** origin allow-list in `src/app/api/quiz-submission/route.ts` hard-coded `localhost:3000` for dev, which broke submission when Next fell back to 3001 (or any alternate port). Widened to accept any localhost/127.0.0.1 origin in non-prod.
- **/quiz loopback kill was premature and has been reverted.** I initially rewrote `src/app/quiz/page.tsx` as a server-side 307 redirect to `/stack` preserving query params. That was wrong: the user pointed out it stripped the intake experience entirely, and the 6 replacement quizzes (todo 10) aren't built yet. Restored the legacy 1625-line general-intake quiz from `HEAD` via `git checkout HEAD -- src/app/quiz/page.tsx src/app/quiz/layout.tsx`. `/quiz` now returns 200 OK again, not a redirect. Sitemap also restored. The loopback cleanup is deferred until the replacement quizzes ship — at that point, the legacy quiz can be replaced inline rather than redirected away.
- **Klaviyo flows doc written** at `docs/klaviyo-flows.md`. Covers: full event-name + list-env-var mapping as shipped; which profile props to segment on; 5 concrete flows (Intake Started nurture, Engagement Tool nurture, Waitlist Welcome, Medical Review escalation, Better-Elsewhere soft bounce); SMS flow compliance with TCPA gating on `halo_sms_consent = true`; 8 useful up-front segments; env vars required on Render; how to test a flow before enabling. Known gaps section documents the homepage-router and post-launch customer-list migration.

### Not started (remaining)
- **6 full clinical intake quizzes:** `/quiz` (general-router), weight-loss, nad, trt, hrt, peptides. **Blocked on clinical requirements** — I need question sets, conditional branching rules, and tier-assignment logic from product/clinical before I can build these without speculation.
- **Confirm real pricing** for NAD+, Weight Loss, Peptides, Sexual Wellness against MSA Schedule E.
- **Update `/privacy`** to reflect SMS data handling.

---

## 4. New plan dropped this session (not started)

A plan file was produced at `/Users/parkerlee/.claude/plans/sprightly-purring-crane.md` titled **"Halo Brand System v2.0 — Complete Visual Overhaul."** Gist:

- Kill all gold-as-accent. Kill per-program color coding. Kill floating pills, sparkles, blobs, glows, shimmer, ring patterns.
- Unify on a sage (`#6B7B6E`) single accent, warm off-white `#F8F7F4` light bg, near-black `#1A1A1F` dark bg for hero + final CTA only.
- Playfair Display for headlines + italic manifesto; Inter for everything else.
- All program cards unified on `#F0EFEC`. Simple 1px `#E5E4E0` dividers. Generous `py-28 md:py-36` rhythm.
- Retain gold **only** on the logo ellipse gradient (Aesop "olive label on amber bottle" effect).
- Files touched: `globals.css`, `page.tsx`, `layout.tsx`, `Navbar.tsx`, `AnnouncementBar.tsx`, `Footer.tsx`, `ChatWidget.tsx`, `FoundingCircleForm.tsx`, `HaloLogo.tsx`.
- Includes Midjourney master suffixes for product + people shots.

Plan is the source of truth if the overhaul is picked up. Read the full file before starting.

---

## 5. Files modified / created this session

**New:**
- `src/components/quiz/QuizCapture.tsx`
- `src/components/quiz/EngagementTool.tsx`
- `src/lib/engagement-tools.ts`
- `src/app/estimators/page.tsx`
- `.claude/launch.json`

**Modified:**
- `src/app/api/quiz-submission/route.ts` — wired Redis reservation
- `src/lib/quiz-client.ts` — `SubmitQuizResult` now carries `position`
- `src/app/stack/StackBuilder.tsx` — reads position from submit response
- `src/app/weight-loss/page.tsx` — mounted `weight_loss_projection` at section 6.5
- `src/app/testosterone-therapy/page.tsx` — mounted `trt_drag` at section 6.5
- `src/app/hormone-therapy/page.tsx` — mounted `hrt_severity` at section 6.5
- `src/app/peptide-therapy/page.tsx` — mounted `peptides_recovery` between "The Solution" and "What Changes"
- `src/app/nad-therapy/page.tsx` — temporarily replaced, then **reverted** to legacy `CellularAgeQuiz`

---

## 6. Known gotchas for the next chat

- **Bash `cwd` resets between invocations** to `/Users/parkerlee/Desktop/Merit Peptides`. Chain with `cd /Users/parkerlee/Desktop/Halo/app && cmd` inside a single Bash call.
- Tier enum is strict — if you see TS errors mentioning `"low" | "medium" | "high"` not assignable, that's the map from local `severity` to `QuizDerived["tier"]`. See pattern in `engagement-tools.ts`.
- `EventName` is `quiz_complete` not `quiz_completed`.
- Zod `.default({})` on `answers` makes it non-optional in output — always pass `answers: answers ?? {}`.
- `/nad-therapy` legacy quiz is intentional. Do not "consolidate" it.

---

## 7. Suggested first move in new chat

Two reasonable entry points:

**A. Finish the QA** (user's last stated intent):
> "Continue the QA task. `.claude/launch.json` is already set up with `halo-dev`. Start the preview server, walk `/estimators`, then one full tool flow into `/stack`, and report any console/network errors."

**B. Start the brand overhaul** (bigger scope, new plan file):
> "Read `/Users/parkerlee/.claude/plans/sprightly-purring-crane.md` and begin the Halo Brand System v2.0 overhaul starting with `globals.css` and `layout.tsx`."

Pick based on what the user asks for first. Don't assume.
